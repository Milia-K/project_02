import React from 'react'
import ContainerCard from '../../Components/Card/ContainerCard'

function Game({appData}) {

  return (
    <div>
      <ContainerCard appData={appData} />
    </div>
  )
}

export default Game